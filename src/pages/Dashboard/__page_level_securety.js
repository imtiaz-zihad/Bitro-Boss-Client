/**
 * 1. do not show the link those user who should not see the link
 * 2. even if they gets link, don;t allow them to access the page
 * 3. do not allow user to access api . check admin in the server as well(verifyAdmin)
 */